---
const today = new Date();
---

<footer>
	&copy; {today.getFullYear()} Digital Marketing Expert. All rights reserved.
	<div class="social-links">
		<h1></h1>
	</div>
</footer>
<style>
	footer {
		padding: 2em 1em 1em 1em; /* Reducido el padding-bottom a 1em */
		background: linear-gradient(var(--gray-gradient)) no-repeat;
		color: rgb(var(--gray));
		text-align: center;
		font-size: 1.2rem; /* Aumenta el tamaño de las letras */
		line-height: 1.4; /* Ajuste de line-height para menos espacio entre líneas */
		margin: 0; /* Eliminar margen extra */
	}

	footer div {
		margin-top: 1em; /* Espaciado entre texto y redes sociales */
	}

	.social-links {
		display: flex;
		justify-content: center;
		gap: 1em;
		margin-top: 0.5em; /* Ajusté el margen superior para acercar más */
	}

	.social-links a {
		text-decoration: none;
		color: rgb(var(--gray));
		font-size: 1.5rem; /* Aumenta el tamaño de los enlaces sociales */
	}

	.social-links a:hover {
		color: rgb(var(--gray-dark));
	}
</style>
