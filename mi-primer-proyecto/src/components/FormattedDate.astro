---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('es-ES', {
			year: 'numeric',
			month: 'long', // Cambia a 'short' si quieres abreviaturas (ej. "ene" en lugar de "enero")
			day: 'numeric',
		})
	}
</time>
